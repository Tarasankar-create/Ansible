---
- name: create pass
  hosts: webhooks
  become: true

  tasks:
    - name: set pass
      user:
        name: nick
        update_password: always
        password: "{{'abc123' | password_hash('sha512')}}"
