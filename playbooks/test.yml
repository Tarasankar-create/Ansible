---
- name: first code
  hosts: webservers
  become: true

  tasks:
  - name: test
    ping:
  - name: "print output"
    debug: msg="Ho!"
  - name: install nginx
    apt:
      name: nginx
      state: present
  - name: starting nginx service
    service:
      name: nginx
      state: started
      enabled: true
