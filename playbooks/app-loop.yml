---

- name: install and start services
  hosts: webhooks
  become: true
  vars:
    apps: [tree,nginx,tmux]
  tasks:
    - name: installing apps
      apt:
        name: "{{ item }}"
        state: present
      tags: i-nginx
      with_items: "{{ apps }}"
